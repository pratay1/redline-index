import { TrimEditor } from "@/components/admin/trim-editor";
import { prisma } from "@/lib/prisma";

export default async function NewTrimPage() { const [modelYears, engines, transmissions, sources] = await Promise.all([prisma.modelYear.findMany({ include: { generation: { include: { model: { include: { manufacturer: true } } } } }, orderBy: { year: "desc" } }), prisma.engine.findMany({ orderBy: { name: "asc" } }), prisma.transmission.findMany({ orderBy: { name: "asc" } }), prisma.source.findMany({ orderBy: { title: "asc" } })]); return <main><p className="font-mono text-[0.65rem] tracking-[0.16em] text-signal uppercase">Trim editor</p><h1 className="mt-3 text-4xl font-semibold text-white">Create trim</h1><TrimEditor modelYears={modelYears.map((x) => ({ id: x.id, label: `${x.year} ${x.generation.model.manufacturer.name} ${x.generation.model.name} ${x.generation.code}` }))} engines={engines.map((x) => ({ id: x.id, label: `${x.name}${x.code ? ` (${x.code})` : ""}` }))} transmissions={transmissions.map((x) => ({ id: x.id, label: x.name }))} sources={sources.map((x) => ({ id: x.id, label: x.title }))} /></main>; }
